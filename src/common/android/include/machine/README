The files in this directory were taken directly from Android NDK r12. They were
intentionally removed from the NDK during the move to unified headers, which
causes Breakpad compilation issues on newer NDK versions.
